GIHUB:
Assignment:

1. Clone a repo using ssh 

2. create a project in gitgub and create one repo inside it

3. setup e mail notification for repo deletion and creation

4. create two branches and merge by raise a pull request with reviewers

5. create a repo and commit files to that repo

6. Explore cherry-pich and use it 

Git lab:

1. Explore Gitlab
2. Explore bitbucket 

NOTE: https://docs.github.com/en/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent
ssh-keygen -f ~/.ssh/id_rsa_sri
export GIT_SSH_COMMAND="ssh -i /root/.ssh/id_rsa_sri"


Interview Questions:
1. What is git rebase
2. git cherry pick
3. git stash
4. What is conflit 
5. What is diff b/w git fetch and git pull
6. what is diff b/w git merge and git rebase 
--------------------------------------------------
GitHub Tools:

1.GitHUB

2.Github Desktop

3.GitHUB Enterprise   